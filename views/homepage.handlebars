<!-- Loop over each object in the posts array -->
{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/post/{{post.id}}">{{post.username}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{post.description}}</span>
      needed!
    </p>
    <p>Created by {{post.user.username}} on {{post.comment.comment}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.date_created}}
    </p>
  </div>
</div>
{{/each}}
